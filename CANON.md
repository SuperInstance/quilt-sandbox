# quilt-sandbox

**What this repo is**: canon lore is verified to RUN, not just read.

Substrate transition in 24 lines:
- lore markdown → extract Python code blocks → subprocess exec
- the canon is canon when it runs
- "the math can BE and grow" — Casey's canon that grows

When canon pieces contain Python, the sandbox verifies that the code
actually executes correctly. Canon that runs is canon that survives.
The substrate walker canon is *executable*, not just *narrative*.

Layered navigation: README.md → docs/SANDBOX_DOCTRINE.md → quilt_sandbox/sandbox.py → tests/test_sandbox.py
